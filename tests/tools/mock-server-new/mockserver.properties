mockserver.serverPort=8091

mockserver.enableCORSForAllResponses=true
mockserver.corsAllowOrigin=*
mockserver.corsAllowMethods=CONNECT, DELETE, GET, HEAD, OPTIONS, POST, PUT, PATCH, TRACE
mockserver.corsAllowHeaders=accept, accept-encoding, authorization, content-type, dnt, origin, user-agent, x-csrftoken, x-requested-with, x-api-key, api-key
mockserver.corsMaxAgeInSeconds=300

mockserver.maxSocketTimeout=120000
mockserver.logLevel=OFF
